using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class UnitWindow : MonoBehaviour {

    //public Text listOfTargets;

	// Use this for initialization
	void Start () {
        
        //listOfTargets.text = "Ennemies Modded";
        //transform.GetChild(0).GetComponent<Text>().text = listOfTargets.text;
    }
	
	// Update is called once per frame
	void Update () {
        
        /*
        foreach (Unit baddie in UnitHandler.targetList)
            listOfTargets.text += baddie.uName;
            */
    }
}
